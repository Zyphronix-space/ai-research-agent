VITE_API_URL=http://localhost:8001

# Optional — only needed for the Google Sign-In button. Same OAuth Client
# ID you set as GOOGLE_CLIENT_ID on the backend (see backend/.env.example).
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
